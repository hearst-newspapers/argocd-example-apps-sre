apiVersion: v1
kind: Service
metadata:
  name: {{ include "redirect-test.fullname" . }}
  labels:
    {{- include "redirect-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redirect-test.selectorLabels" . | nindent 4 }}
