apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redirect-test.fullname" . }}-test-connection"
  labels:
    {{- include "redirect-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redirect-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
