apiVersion: v1
kind: Service
metadata:
  name: {{ include "guestbook-sre.fullname" . }}
  labels:
    {{- include "guestbook-sre.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "guestbook-sre.selectorLabels" . | nindent 4 }}
